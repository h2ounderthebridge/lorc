#ifndef LAYER_H
#define LAYER_H

#include <vector>
#include "Neuron.h"

class Layer
{
 public:
  Layer(std::vector<Neuron> neurons);
  std::vector<double> getlayeroutputs(std::vector<double> inputs);
  std::vector<Neuron> m_neurons;
};

#endif
